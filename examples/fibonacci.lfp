(program Fibonacci
  (function fib ((n Integer)) Integer
    (if (<= n 1)
        n
        (+ (fib (- n 1)) (fib (- n 2)))))

  (begin
    (for (i 0 to 12)
      (writeln "fib(" i ") = " (fib i)))))
